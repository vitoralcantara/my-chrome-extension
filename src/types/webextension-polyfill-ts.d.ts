declare module 'webextension-polyfill-ts' {
    // Minimal declaration to satisfy imports in this project.
    // Using `any` to avoid requiring external @types packages.
    export const Runtime: any;
}
